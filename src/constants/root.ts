export const ENDPOINTS = {
    POST: {
        IMAGE_TO_VIDEO: `${process.env.REACT_APP_API_URL}/image_to_video`,
    },
    GET: {
        POLL_TASK: `${process.env.REACT_APP_API_URL}/poll_task`,
    }
}